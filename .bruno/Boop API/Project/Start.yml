info:
  name: Start
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8004/boop/api/projects/{{PROJECT}}/start
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
