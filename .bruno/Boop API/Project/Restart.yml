info:
  name: Restart
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:8004/boop/api/projects/{{PROJECT}}/restart
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
