info:
  name: List deploy
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8004/boop/api/projects/{{PROJECT}}/logs/deploy
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
