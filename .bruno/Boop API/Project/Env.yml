info:
  name: Env
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:8004/boop/api/projects/{{PROJECT}}/env
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
